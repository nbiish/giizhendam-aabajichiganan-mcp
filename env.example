# Copy to .env and fill the values. These are read by your MCP config via the "env" block or your shell environment.

# OpenRouter (primary orchestrator API)
OPENROUTER_API_KEY=
# Orchestrator model used for execution planning and final synthesis
# Default: google/gemini-2.5-pro (override if needed)
ORCHESTRATOR_MODEL=google/gemini-2.5-pro

# Optional: OpenRouter timeout (ms) for chat completions
# Default: 30000
OPENROUTER_TIMEOUT_MS=30000

# Optional: direct Gemini API (not required if using OpenRouter exclusively)
# GEMINI_API_KEY=
# GEMINI_MODEL=gemini-1.5-flash-latest

# Agents configuration
# Preferred: JSON array of agent definitions. If unset, the server will fallback to parsing llms.txt
# Example format:
# CLI_AGENTS_JSON=[
#   {"name":"Qwen","cmd":"qwen -y \"{prompt}\""},
#   {"name":"Gemini","cmd":"gemini -y \"{prompt}\""},
#   {"name":"Cursor","cmd":"cursor agent --print --approve-mcps \"{prompt}\""},
#   {"name":"Goose","cmd":"echo \"{prompt}\" | goose"},
#   {"name":"Opencode","cmd":"opencode run \"{prompt}\""},
#   {"name":"Crush","cmd":"crush run \"{prompt}\""}
# ]
CLI_AGENTS_JSON=

# Output directory where agent artifacts will be written
AGENT_OUTPUT_DIR=./output/agents

# Optional: force a specific execution style instead of model selection
# Allowed: sequential | parallel | pipeline | feedback | auto
EXECUTION_STYLE=auto

# Synthesis size caps (to avoid timeouts / context overflow)
# Max characters to include per agent file when synthesizing
SYNTH_MAX_PER_AGENT_CHARS=20000
# Max total characters across all agent sections
SYNTH_MAX_TOTAL_CHARS=150000
